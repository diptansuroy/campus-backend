database:
  type: sqlite
  host: localhost
  user: postgres
  password: password
  name: campus_db
  port: "5432"

server:
  port: "8080"
  gin_mode: debug

jwt:
  secret: your-super-secret-jwt-key

email:
  smtp_host: smtp.gmail.com
  smtp_port: "587"
  smtp_username: ""
  smtp_password: ""
  from_email: noreply@campus.edu
